# Prototype Instructions

Run the local server yourself and open the preview in the in-app browser. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

HITO Design must not be reduced to a short portfolio. Future edits should preserve a strong identity narrative around the human, method and real-world delivery, keep Design global visible as a core mission, and distinguish truthful built references from broader fields of intervention and transmission.

The user supplied the VAE memoir and the book `Transmettre mon métier AI Designer`; use them as editorial source material for Chris Reati’s career, method and convictions. Keep the core positioning around doubt as a tool, method as structure, technical reality, transmission, and “traduire la complexité humaine en espace habitable”.

The main site should act as the curated showcase and professional conversion path. The existing Google Sites should become “Les Carnets HITO”: a long-form blog/reference space for detailed case studies, method, career, teaching and project context.

Client-reference logos have been supplied and converted into `public/assets/clients/` and `dist/assets/clients/`. Do not replace them with invented logos or generic placeholders.

Do not publish private project names or details found in the memoir unless the user explicitly approves them. When unsure, present them as broader mission typologies rather than named case studies.
